# Makefile for mandelbrot area code

#
# C compiler and options for Cray/clang 
#
CC=     cc -O3 -fopenmp -std=c99
LIB=    -lm

# C compiler and options for GNU 
#
#CC=     cc -O3 -fopenmp
#LIB=	-lm

#
# Object files
#
OBJ=    area_task_omp.o

#
# Compile
#
area_task_omp.x:   $(OBJ)
	$(CC) -o $@ $(OBJ) $(LIB)

.c.o:
	$(CC) -c $<

#
# Clean out object files and the executable.
#
clean:
	rm *.o area_task_omp.x
